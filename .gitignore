node_modules/
.next/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Source copies are preserved locally; the application reads the curated copies
# in /public so these duplicates never enter the fresh repository snapshot.
/ACHIEVEMENTS/
/GENERAL_ASSEMBLY/
/NATGESS/
/Officers/
/UNIFORMS/
/GESS_Logo.jpg
/figmasourcedesign/
/Design2/
**/*.fig

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
